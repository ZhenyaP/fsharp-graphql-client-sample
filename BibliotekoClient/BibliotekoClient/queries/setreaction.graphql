mutation setReaction(
    $isbn: ISBN!
    $recenzoId: ID!
    $reactionKind: ReactionEnum!
    $uzantoId: String!
)
{
    setReaction(
        isbn: $isbn
        recenzoId: $recenzoId
        reactionKind: $reactionKind
        uzantoId: $uzantoId
    )
    {
        id
        recenzorer
        content {
            ... on Reaction {
                reaction
            }
            ... on Comment {
                comment
            }
            ... on ReactionAndComment {
                reaction
                comment
            }
        }
    }
}