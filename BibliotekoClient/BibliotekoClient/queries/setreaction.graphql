mutation setReaction(
    $isbn: ISBN!
    $recenzoId: RecenzoID!
    $reactionKind: ReactionEnum!
    $uzantoId: UzantoID!
)
{
    setReaction(
        isbn: $isbn
        recenzoId: $recenzoId
        reactionKind: $reactionKind
        uzantoId: $uzantoId
    )
    {
        id
        recenzorer
        content {
            ... on Reaction {
                reaction
            }
            ... on CommentOnly {
                comment
            }
            ... on ReactionAndComment {
                reaction
                comment
            }
        }
    }
}